// Version stamp. THIS FILE IS THE DEV PLACEHOLDER: build.mjs overwrites it in dist/ with
// the real commit and build time, so the deployed app always says exactly what it is —
// which is the only reliable answer to "is Chrome showing me a cached build?".
window.ONELAKE_STUDIO_VERSION = { commit: "dev", builtAt: "" };
